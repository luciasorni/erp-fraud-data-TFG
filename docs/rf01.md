# RF01 - Estado

## Alcance

Ingesta reproducible de datos P2P a DuckDB con evidencias de ejecución.

## Entregado

- Estructura base de proyecto (`ingest`, `storage`, `cli`)
- Convención de salida `run_results/<run_id>/`
- Lectura/validación de zip y ficheros esperados
- Carga tabular robusta (CSV/Parquet)
- Normalización de tipos + limpieza técnica
- Carga a DuckDB (`overwrite`/`append`)
- Métricas por tabla de carga
- `dataset_hash` reproducible
- `schema_summary.json`
- `run_metadata.json`
- Logging JSONL de ingesta
- Manejo de errores típicos
- Tests unitarios RF01
- Verificación de aceptación RF01 (`run_results/rf01-18-verification-20260224/`)

## Verificación rápida

```bash
/opt/anaconda3/bin/python -m pytest -q tests/test_rf01_ingest_storage.py
```
