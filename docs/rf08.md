# RF08 - Estado

## Alcance

Generar reporte final en formato estructurado (`report.json`) y legible (`report.md`/`report.html`) con enlaces a evidencias.

## RF08-01 completado

Se definió la estructura base de `report.json` en:

- `src/erp_fraud/storage/report_json.py`

Componentes:

- `REPORT_JSON_VERSION`
- `build_report_json_payload(...)`
- `write_report_json(...)`

Estructura incluida:

- `report_version`, `generated_at_utc`
- `metadata`:
  - `run_id`
  - `dataset_hash`
  - `metadata_extra`
- `summary`:
  - estado global
  - recuento de tests (`ok/error/timeout`)
  - recuento de hallazgos
  - `top_k`
- `ranking`:
  - `top_k`
  - `row_count`
  - `rows`
- `test_runs` (estado por test)
- `artifact_paths` (rutas a evidencias)
- `errors` (errores estructurados)

Objetivo de esta tarea:

- dejar contrato estable del JSON para que RF08-02/03 construyan MD/HTML encima de esta salida.

## Siguiente tarea

## RF08-02 completado

Se añadió plantilla fija de `report.md` en:

- `src/erp_fraud/storage/reporting.py`

Funciones:

- `build_report_markdown_template(run_id, dataset_hash)`
- `write_report_markdown_template(report_md_path, run_id, dataset_hash, overwrite=False)`

Secciones incluidas en plantilla:

- `Resumen`
- `Estado de tests`
- `Top-k sospechosos`
- `Anexos por test`
- `Errores`

## Siguiente tarea

## RF08-03 completado

Se implementó generación de Markdown desde el JSON estructurado:

- `src/erp_fraud/storage/reporting.py`
  - `build_report_markdown_from_report_json_payload(report_payload)`
  - `write_report_markdown_from_report_json(report_json_path, report_md_path=None)`

Comportamiento:

- lee `report.json`
- renderiza secciones fijas:
  - `Resumen`
  - `Estado de tests`
  - `Top-k sospechosos`
  - `Anexos por test`
  - `Errores`
- guarda `report.md` (por defecto junto a `report.json`).

## Siguiente tarea

## RF08-04 completado

Se añadió render opcional de `report.md` a `report.html`:

- `src/erp_fraud/storage/reporting.py`
  - `render_report_markdown_to_html(report_md_path, report_html_path=None)`

Comportamiento:

- si existe librería `markdown`, genera HTML renderizado (tablas/código)
- si no existe, usa fallback seguro (`<pre>`) para no bloquear el pipeline
- por defecto escribe junto al MD (`report.html`)

## Siguiente tarea

## RF08-05 completado

Se añadieron rutas/enlaces automáticos de evidencias en `report.json`:

- `src/erp_fraud/storage/report_json.py`
  - `build_default_report_artifact_paths(run_id, test_runs, sample_top_n=20)`
  - integración en `build_report_json_payload(...)`

Rutas incluidas:

- salidas globales:
  - `report_json`, `report_md`, `report_html`
  - `ranking_json`, `ranking_parquet`
  - `test_runs_json`
  - `data_validation_report_json`
- diccionario y esquema:
  - `data_dictionary_json`
  - `data_dictionary_md`
  - `schema_summary_json`
- por test (`tests_outputs.<test_id>.*`):
  - `findings_jsonl`
  - `findings_parquet`
  - `sample_json`

## Siguiente tarea

## RF08-06 completado

Se añadió manejo explícito de errores de tests fallidos/timeouts:

- `src/erp_fraud/storage/report_json.py`
  - genera errores automáticos desde `test_runs` con estado `ERROR`/`TIMEOUT`
  - formato: `scope=test_run`, `code=<STATUS>:<TEST_ID>`, `message=error_summary`
- `src/erp_fraud/storage/reporting.py`
  - sección `Errores` en MD incluye tabla específica:
    - `test_id`
    - `status`
    - `error_summary`

Resultado:

- `report.json` contiene `errors` consistentes aunque no se pasen manualmente
- `report.md` refleja fallos/timeouts de forma legible para auditoría

## Siguiente tarea

## RF08-07 completado

Se añadió validación de links/rutas de artefactos en `report.json`:

- `src/erp_fraud/storage/report_json.py`
  - `validate_report_artifact_paths_exist(report_payload, base_path='.')`
  - `validate_report_json_file_artifact_links(report_json_path, base_path='.')`

Comportamiento:

- inspecciona `artifact_paths` del reporte
- comprueba existencia de cada fichero referenciado
- devuelve lista estructurada de faltantes (`artifact_key`, `path`)
- permite bloquear publicación del reporte si hay links rotos

## Siguiente tarea

## RF08-08 completado

Se añadieron pruebas unitarias de reporte en:

- `tests/test_rf08_reporting.py`

Cobertura:

- estructura base de `report.json`
- generación automática de `errors` desde `test_runs`
- generación de `report.md` desde `report.json`
- render opcional `report.md -> report.html`
- validación de `artifact_paths`:
  - detección de links rotos
  - caso completo sin faltantes

Ejecución:

- `pytest -q tests/test_rf08_reporting.py`
- resultado: `6 passed`

## Siguiente tarea

## RF08-09 completado

Se actualizó documentación general para RF08:

- `README.md`
  - sección `Reporte MVP (RF08)` con comportamiento, componentes y validación de links
  - comando de verificación `tests/test_rf08_reporting.py`
- `docs/how_to_run.md`
  - comando de tests RF08
  - listado de artefactos de reporte (`report.json`, `report.md`, `report.html`)

## Siguiente tarea

## RF08-10 completado

Se ejecutó verificación final de criterios de aceptación y se guardó evidencia en:

- `run_results/rf08-10-verification-20260310/`

Artefactos clave:

- `pytest_rf08.stdout.log`
- `pytest_rf08.stderr.log`
- `report.json`
- `report.md`
- `report.html`
- `report_links_validation.json`
- `rf08_10_runtime_summary.json`
- `rf08_10_acceptance_verification_report.json`

Resultado:

- `pytest`: `6 passed`
- `report.json`/`report.md`/`report.html` generados
- validación de links de artefactos: `missing_artifacts = 0`

## Siguiente tarea

- RF08 cerrado.
