# RF03 - Estado

> Nota de estado: este documento describe la entrega inicial de RF03. El catálogo vigente es más amplio: 10 pruebas P2P en `tests/catalog`, 7 pruebas O2C en `tests/catalog_o2c` y resumen consolidado en `docs/catalog.md`.

## Alcance

Entrega inicial de catálogo versionado de tests de fraude con 2 controles P2P y salida estándar. El estado final del catálogo está consolidado en `docs/catalog.md`.

## Entregado

- Esquema `TestSpec` v1:
  - `src/erp_fraud/catalog/test_spec_schema.py`
  - documentación en `docs/tests.md`
- Loader con validación de esquema:
  - `src/erp_fraud/catalog/test_spec_loader.py`
- Catálogo inicial (`tests/catalog`):
  - `tst_duplicate_postings.yaml`
  - `tst_unusual_amount_by_vendor.yaml`
- Lógica implementada de ambos tests:
  - `src/erp_fraud/catalog/test_execution.py`
  - SQL de referencia en `sql/tests/`
- Requisitos de datos exactos por test (`required_columns_exact`) para validación técnica previa (RF02b)
- Versionado y trazabilidad del catálogo:
  - `tests/CHANGELOG.md`

## Tests implementados

1. `TST-DUPLICATE-POSTINGS` `v1.0.0`
   - Riesgo: `duplicate_payment`
   - Tabla: `fraud_1`
   - Columnas: `Kreditor`, `Belegnummer`, `Position`, `Betrag`
2. `TST-UNUSUAL-AMOUNT-BY-VENDOR` `v1.0.0`
   - Riesgo: `amount_anomaly`
   - Tabla: `fraud_1`
   - Columnas: `Kreditor`, `Betrag`, `Transaktionsart`

## Esquema estándar de salida

Cada ejecución devuelve:

- `result_schema_version`
- `generated_at_utc`
- `test_id`, `test_version`, `fraud_type`
- `status`, `finding_count`, `duration_ms`
- `columns`, `rows`
- `metadata` (`implementation_type`, `executed_on`)

## Verificación rápida

```bash
/opt/anaconda3/bin/python -m pytest -q tests/test_rf03_catalog.py
```

Resultado esperado actual:
- `4 passed`
