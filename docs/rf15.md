# RF15 — LLM experto en tests + explicaciones (guardrails anti-alucinación)

## Estado

RF15 queda implementado y verificado hasta `RF15-10`.

## Qué se implementó

- `RF15-01` — contrato de explicación:
  - `src/erp_fraud/catalog/explanation_schema.py`
  - export en `src/erp_fraud/catalog/__init__.py`
- `RF15-02` — prompt con guardrails explícitos:
  - `prompts/explainer.md`
  - versión: `prompts/explainer__v002.md`
- `RF15-03` — generación por entidad (top-k ranking) + resumen de run:
  - `src/erp_fraud/graph/nodes.py` (`explainer_node`)
- `RF15-04` — validación de referencias contra catálogo + schema + findings:
  - `src/erp_fraud/graph/nodes.py` (`_validate_explanations_guardrails`)
- `RF15-05` — loop de autocorrección con feedback estructurado:
  - `src/erp_fraud/graph/nodes.py` (`explainer_last_repair_feedback`)
- `RF15-06` — persistencia y enlace en reporte:
  - artefactos `graph/explanation.json` y `graph/explanation.md` (más alias plural)
  - sección de explicaciones en `report.md`
- `RF15-07` — tests de validador + snapshot de plantilla:
  - `tests/test_rf15_validator_and_snapshot.py`
  - `tests/fixtures/rf15/explanation_template_snapshot.md`
- `RF15-08` — ampliación de tests unitarios RF15:
  - tests de guardrails, repair, persistencia y reporte
- `RF15-09` — documentación operativa actualizada:
  - `README.md`
  - `docs/how_to_run.md`
  - este documento (`docs/rf15.md`)
- `RF15-10` — verificación ejecutada y evidencia:
  - `docs/rf15_verification.md`

## Artefactos RF15 por run

En `run_results/<run_id>/graph/`:

- `explanation.json`
- `explanation.md`
- `explanations.json` (compatibilidad)
- `explanations.md` (compatibilidad)

En `run_results/<run_id>/`:

- `report.json` (incluye `artifact_paths` de explicaciones)
- `report.md` (sección `Explicaciones del agente`)

## Nota LangSmith

RF15 no requiere LangSmith para cerrar aceptación en local.  
Si no está configurado, la trazabilidad queda en artefactos locales y tests.
